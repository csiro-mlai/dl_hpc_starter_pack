defaults:
  - paths/hpc_paths@_global_
  - cluster/2hr@_global_
  - _self_

module: task.cifar10.model.baseline
definition: Baseline
monitor: 'val_acc'
monitor_mode: 'max'

lr: 1e-3
max_epochs: 3
mbatch_size: 32
